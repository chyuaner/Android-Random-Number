<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.blogspot.yuan817.random.number"
    android:versionCode="12"
    android:versionName="2.2" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" 
            android:windowSoftInputMode="stateHidden|adjustResize" android:theme="@style/activity_main_Theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="LottedList" android:label="@string/title_lotted_list" android:theme="@style/lotted_list_Theme"></activity>
    </application>

</manifest>